version: '3'
services:
  nextjs:
    image: nextjs-app
    build: .
    container_name: psycholess-site
    restart: always
    ports:
      - "3000:3000"

  nginx:
    image: nginx
    build:
      context: ./nginx
    container_name: psycholess-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./certbot/www:/var/www/certbot
      - ./certbot/conf:/etc/letsencrypt
    depends_on:
      - psycholess-site
    restart: unless-stopped

version: '3'
services:
  psycholess-site:
    build: .
    container_name: psycholess-site
    restart: always
    expose:
      - "3000"
    networks:
      - web_network

networks:
  web_network:
    external: true
